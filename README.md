# Join-3222


# up.bat und up.sh
# Windows
# Im Terminal kann über up.bat CommitName oder ./up.bat CommitName direkt alle änderungen auf den aktuellen branch gepusht werden
# Mac
# Im Terminal kann über up.sh CommitName oder ./up.sh CommitName direkt alle änderungen auf den aktuellen branch gepusht werden